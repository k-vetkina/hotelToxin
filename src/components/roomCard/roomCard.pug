block variables 

.roomCard(class={'mainSearch__roomCard': mainSearch})
  
  .roomCard__pic 
  include ../roomHeader/roomHeader 
  +roomHeader('roomHeader__roomCard', '888', 'люкс', '9 990₽', 'в сутки')
    
  .roomCard__line
  .roomCard__bottom
    .roomCard__rateBtns
      include ../rateBtn/rateBtn
      +rateBtn('colored', 'star')
      +rateBtn('colored', 'star')
      +rateBtn('colored', 'star')
      +rateBtn('colored', 'star')
      +rateBtn('colored', 'star')
    p.roomCard__halfData
      span.roomCard__cost 145 
      span.roomCard__feedback Отзывов
          